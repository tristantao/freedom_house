%div.row-fluid
  %div.span2
    = render 'admin/shared/sidebar'
    
  %div.span10
    %div.well
      %legend.pagination-centered Create New Event
      = simple_form_for(:event, :as => :event_name, :url => admin_events_action_path(:new), :html => { :method => :put, :class => 'form-horizontal'}) do |f|
        = f.error_notification
        .form-inputs
          = f.input :name, :input_html => { :class => "span9" }
          = f.input :date, :input_html => { :class => "span3" }
          = f.input :longitude, :input_html => { :class => "span9" }
          = f.input :latitude, :input_html => { :class => "span9" }
          = f.input :country, :input_html => { :class => "span9" }
          = f.input :province, :input_html => { :class => "span9" }
          = f.input :city, :input_html => { :class => "span9" }
          = f.input :description, :as => :text, :input_html => { :class => "span9", :rows => "10" }
        .form-actions
          %div.btn-group
            = f.button :submit, "Create", :class => "btn-success"
            = link_to 'Cancel', admin_events_path, :class => "btn btn-danger"


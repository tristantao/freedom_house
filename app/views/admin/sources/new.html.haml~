%div.row-fluid
  %div.span2
    = render 'admin/shared/sidebar'
    
  %div.span10
    %div.well
      %legend.pagination-centered Create New Source
      = simple_form_for(:source, :as => :source_name, :url => admin_sources_action_path(:new), :html => { :method => :put, :class => 'form-horizontal'} ) do |f|
        = f.error_notification
        .form-inputs
          = f.input :name, :required => true, :input_html => { :class => "span8" }
          = f.input :home_page, :required => true, :input_html => { :class => "span8" }
          = f.input :quality_rating, :required => false, :input_html => { :class => "span8" }
        .form-actions
          = f.button :submit, "Create", :class => "btn btn-success"

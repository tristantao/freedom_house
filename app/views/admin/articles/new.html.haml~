%div.row-fluid
  %div.span2
    = render 'admin/shared/sidebar'
    
  %div.span10
    %div.well


      %legend.pagination-centered Create New Article
      = simple_form_for(:article, :as => :article_name, :url => admin_articles_action_path(:new), :html => {:method => :put, :class => 'form-horizontal'}) do |f|
        = f.error_notification
        .form-inputs
          = f.input :title, :required => true
          = f.input :date, :required => true
          = f.input :author, :required => false
          = f.input :location, :required => false
          = f.input :link, :required => true
          = f.input :text, :required => true
        .form-actions
          %div.btn-group
            = f.button :submit, "Create", :class => "btn btn-success"
            = link_to 'Cancel', admin_articles_path, :class => "btn btn-danger"
